server:
  port: 8080

anthropic:
  api-key: ${ANTHROPIC_API_KEY}
  model: claude-sonnet-4-6
  max-tokens: 2048

gemini:
  api-key: ${GEMINI_API_KEY}
  model: gemini-1.5-flash

openai:
  api-key: ${OPENAI_API_KEY}
  model: gpt-4o-mini

spring:
  application:
    name: orchestration-service
# AI Orchestration Gate Test
